# AI Chief of Staff - Environment Configuration
# Copy this file to .env and fill in your actual values

# =====================================================
# SYSTEM CONFIGURATION
# =====================================================

# Base directory for data storage (optional)
# If not set, will use the project root directory
AICOS_BASE_DIR=/path/to/your/data/directory

# Test mode - bypasses credential validation for testing
# Set to "true" for development/testing, "false" for production
AICOS_TEST_MODE=false

# =====================================================
# AUTHENTICATION SYSTEM (Encrypted Key Manager)
# =====================================================

# NOTE: API tokens are stored securely in an encrypted database at src/core/encrypted_keys.db
# Use the setup tools instead of environment variables for production credentials:
#
# For Slack authentication:
#   python tools/setup_slack_auth.py
#
# For Google OAuth setup:  
#   python tools/setup_google_oauth.py
#
# For testing purposes only, you can set these environment variables:
# SLACK_BOT_TOKEN=xoxb-test-token (when AICOS_TEST_MODE=true)
# GOOGLE_CLIENT_ID=test-client-id (when AICOS_TEST_MODE=true)
# GOOGLE_CLIENT_SECRET=test-secret (when AICOS_TEST_MODE=true)

# =====================================================
# ENCRYPTED KEY MANAGER CONFIGURATION
# =====================================================

# Master encryption key environment variable name (optional)
# If not set, will use default key file at src/core/.master_key
# AICOS_MASTER_KEY_ENV=MASTER_ENCRYPTION_KEY

# Key database location (optional)
# If not set, will use default location at src/core/encrypted_keys.db
# AICOS_KEY_DB_PATH=/custom/path/to/encrypted_keys.db

# =====================================================
# AI/LLM CONFIGURATION (Optional)
# =====================================================

# Anthropic API Key (for Claude)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API Key (for GPT models)
OPENAI_API_KEY=your-openai-api-key-here

# =====================================================
# OPTIONAL CONFIGURATION
# =====================================================

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Data retention period in days (default: 365)
DATA_RETENTION_DAYS=365

# Compression age in days (files older than this get compressed)
COMPRESSION_AGE_DAYS=30

# Authentication cache timeout in seconds (default: 3600)
AUTH_CACHE_TIMEOUT=3600